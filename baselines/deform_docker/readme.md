# To Run the code using the docker, use this document 

To run the docker container just run the sh file 

- cd deform_docker && ./run.sh 

Once inside the container 

- source /opt/ros/noetic/setup.bash